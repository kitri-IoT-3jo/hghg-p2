<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../includes/board')-%>
    <%- include('../includes/nav')-%>
    <title>ㅎㄲㅎㄲ 게시판</title>
    <script type="text/javascript">
    window.onload = function() {
        document.getElementById('write').onclick = function() {
            location.href = '/board/write';
        }
    }
    </script>
    <style type="text/css">
      .clear-fix{
        height: 100px;
      }
      .pagebtns {
          display: inline-block;
          height: 24px;
          width: 20px;
          font-size: 1em;
      }
      .nextgroup{
          display: inline-block;
          height: 24px;
          width: 50px;
          font-size: 1em;
      }
      .pagebtns[href='/board/page=<%=cpage%>']{
          color: white;
          background-color: #ffc107;
          border-radius: 5px
      }
    </style>
</head>

<body>
    <!-- Navigation -->
    <%- include('../navbar/nav_main', {user:user})%>
    <div class="clear-fix"></div>
    <div class="container">
        <h3>전체글보기</h3><br>
        <table class="table table-hover">
            <colgroup>
                <col width="80px">
                <col width="*">
                <col width="120px">
                <col width="80px">
                <col width="80px">
            </colgroup>
            <thead>
                <tr>
                    <th>글번호</th>
                    <th>제목</th>
                    <th>작성자</th>
                    <th>작성일</th>
                    <th>조회수</th>
                </tr>
            </thead>
            <tbody>
                <% query.forEach((q)=>{%>
                <tr>
                    <td><%= q.board_id%></td>
                    <td><a href="/board/<%= q.board_id-%>"><%= q.subject%></a></td>
                    <td><%= q.user_nickname%></td>
                    <td><%= q.date%></td>
                    <td><%= q.hit%></td>
                </tr>
                <% });%>
            </tbody>
        </table>
        <div class="text-right">
            <button type="button" class="btn btn-warning" id="write">글쓰기</button>
        </div>
        <div class="text-center"><%- include('./board_buttons', {pages:pages, cpage:cpage})%></div>
        <div>
            <form method="get" action="" enctype="multipart/form-data" class="float-left" novalidate>
                <select name="searchType">
                    <option value="subject"> 제목 </option>
                    <option value="name"> 작성자 </option>
                </select> 
                <input type="text" id="searchText" name="searchText"> <button type="submit" class="btn">검색</button>
            </form> 
        </div>
    </div>
</body>

</html>